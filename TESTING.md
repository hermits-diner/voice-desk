# VoiceDesk — 설치 테스트 체크리스트

설치본(`app\src-tauri\target\release\bundle\nsis\VoiceDesk_0.1.0_x64-setup.exe`)을
깨끗한 Windows 11 에서 검증할 때 순서대로 확인한다.

> 이 앱은 개인용이라 설치본에는 **앱 셸만** 들어 있다. 백엔드(venv · 모델 · ffmpeg)는
> `C:\ai` 아래에 별도로 있어야 하며, 재현 절차는 `SETUP.md` 8절에 있다.
> 앱은 백엔드를 `C:\ai\voice-desk\backend` 에서 찾는다
> (환경 변수 `VOICEDESK_BACKEND` 로 바꿀 수 있다).

## A. 사전 조건 없는 상태 (백엔드 없이 앱만 설치)

- [ ] 설치가 관리자 권한 없이 끝난다 (`currentUser` 모드)
- [ ] 첫 실행 시 크래시 없이 창이 뜬다
- [ ] "백엔드가 응답하지 않습니다" 화면이 뜨고, 원인 문구에
      `파이썬 환경이 없습니다: C:\ai\voice-desk\backend\.venv\...` 가 보인다
- [ ] [로그 보기] 로 stderr 를 펼칠 수 있다
- [ ] 창을 닫으면 프로세스가 남지 않는다 (작업 관리자에서 voice-desk 검색)

## B. 백엔드 구성 후 (SETUP.md 8절 수행 뒤)

### 기동 · 종료
- [ ] 앱 실행 → 1초 안에 온보딩 또는 메인 화면 (백엔드 임포트 0.5초 확인)
- [ ] `voice-desk.exe` 아래에 `python.exe` 가 자식으로 붙어 있다
- [ ] 창 닫기 → python 이 함께 사라지고 GPU 메모리가 돌아온다
- [ ] **작업 관리자에서 voice-desk 를 강제 종료** → python 도 함께 사라진다 (Job Object)
- [ ] 다시 실행하면 창 크기 · 위치가 기억되어 있다

### 온보딩 (첫 실행)
- [ ] Gemini 키 없는 상태: 1단계에 "키 없음" 램프
- [ ] 키 저장 → 자격 증명 관리자(`제어판 → 자격 증명 관리자 → Windows 자격 증명`)에
      `voice-desk` 항목이 생기고, `backend\settings.json` 에는 키가 없다
- [ ] GPU · 모델 · ffmpeg 네 항목이 초록 램프
- [ ] 샘플 만들기 → 1분 안에 완료 램프
- [ ] 모델 폴더 이름을 임시로 바꾸고 [다시 확인] → 해당 줄이 빨간 램프 + 안내 문구
      (크래시 없음)

### 3단계 흐름
- [ ] 주제 입력 → Ctrl+Enter → 대본 카드가 뜬다 (Gemini 과부하 시
      "서버가 붐빕니다" 한 문장, 원본 JSON 노출 없음)
- [ ] 대본에서: 줄 수정 · 삭제 · 순서 이동 · 이 줄만 다시 생성 · 이 줄만 들어보기
- [ ] 화자별 보이스 교체와 미리듣기
- [ ] 음성 만들기 → 파형이 왼쪽에서 자라고, 레일이 순차 점등, 남은 시간이 숫자로
- [ ] 완료 → mp3 · txt · json 세 파일이 출력 폴더에 나란히 생긴다
- [ ] json 의 구간 시각을 두세 개 골라 실제 오디오와 귀로 대조
- [ ] [폴더 열기] → 탐색기에서 mp3 가 선택된 채 열린다
- [ ] 레일 칸 클릭 → 그 구간부터 재생. LOOP → 구간 반복
- [ ] Space 재생/정지, Ctrl+S 폴더 열기

### 히스토리
- [ ] 렌더 완료 항목을 다시 열면 파형 · 구간 · 재생이 그대로 복원
- [ ] 대본만 있는 항목은 2단계로 열린다
- [ ] 항목 삭제 후 앱 재시작 → 지워진 채 유지

### 엔진 전환 · Dia2
- [ ] 설정에서 Dia2 로 전환 → 다음 렌더에서 VibeVoice 가 내려가고 Dia2 가 올라온다
      (VRAM 5.0 → 7.6 GB 부근)
- [ ] 2인 대본을 Dia2 로 렌더 → 완료
- [ ] 한 줄만 고쳐 재렌더 → 눈에 띄게 빨리 끝난다 (캐시. 실측 25.2s → 3.3s)
- [ ] 3인 대본에서 Dia2 선택 → 렌더 전에 안내가 뜨고 VibeVoice 전환 버튼 제공

### 오류 상태
- [ ] Gemini 키를 일부러 틀리게 저장 → "키가 거부됐습니다" 인라인 안내 (크래시 없음)
- [ ] 렌더 중 [중지] → cancelled 로 끝나고 대본은 그대로
- [ ] 백엔드 프로세스를 강제 종료 → "백엔드가 응답하지 않습니다" + [다시 시작] 으로 복구
- [ ] 포트 충돌 (다른 프로그램이 점유) → 로그에 "포트 N 을 이미 다른 프로그램이…"
      한 줄이 남고 설정에서 포트 변경으로 복구

### 빠른 변환
- [ ] 텍스트 → 단일 화자 mp3, 파형과 폴더 열기 동작

### 표시 · 접근성
- [ ] Windows 다크/라이트 전환 → 앱이 따라온다 (시스템 따라가기일 때)
- [ ] Tab 만으로 주제 입력부터 렌더까지 도달 가능, 포커스 링이 항상 보인다
- [ ] Windows 텍스트 배율 150% 에서 레이아웃이 깨지지 않는다
- [ ] 창 최소 크기(960×640)에서 가로 스크롤이 생기지 않는다

### 확장 기능 (2026-09-01 2차)

- [ ] 렌더 완료 시 .srt / .vtt 가 mp3 옆에 생기고, 번역이 둘째 줄로 들어 있다
- [ ] 렌더 중 [이어 듣기] → 합성된 부분부터 소리가 나고, 완료되면 자연히 멈춘다
- [ ] 재생 속도 버튼 1×→0.75→0.5→1.25→1.5 순환, 음정 유지
- [ ] 대본 생성 시 세그먼트마다 한국어 번역이 붙고, 발화에는 섞이지 않는다
- [ ] 보이스 미리듣기 두 번째부터 즉시 재생 (캐시)
- [ ] Supertonic 엔진: 한국어 대본 렌더 정상, VRAM 변화 없음(CPU), 44.1kHz 유지
- [ ] Anki 버튼 → .apkg 생성, Anki 에서 열면 구간 오디오 카드가 재생된다
- [ ] 온보딩 [내려받기] → 진행률 % 가 오르고 완료 후 램프가 초록으로
- [ ] [다음 화] → 1단계로 가고, 생성된 대본이 같은 인물로 이어진다
- [ ] 렌더 중 다른 대본 [음성 만들기] → 우하단 QUEUE 패널에 쌓이고 순서대로 완료
- [ ] Dia2 [클로닝] → wav 선택 후 렌더 → 레퍼런스 목소리로 나온다 (첫 회는 whisper 로드로 느림)
- [ ] 구간 [여기부터 다시 렌더] → 앞부분 유지, 이음새 클릭음 없음 (실험 기능)
- [ ] 구간 마이크 버튼 → 발음 연습 창: 듣기 · 녹음 · 비교 동작
- [ ] 빠른 변환에서 Supertonic + 한국어 텍스트 정상
- [ ] 포트 점유 상태에서 실행 → 자동으로 다음 포트로 물러나 정상 기동

## C. 회귀 확인 (수정 후 반복 실행용)

| # | 항목 | 기대값 | 실측 (2026-09-01) |
|---|---|---|---|
| 1 | Narration + 3인 Conversation → mp3 · json | 둘 다 생성 | ✅ |
| 2 | 410단어 렌더 속도 타당성 | 2.3~3.5 단어/초 | 2.92 |
| 3 | 5분 이상 3인 대화, 화자 유지 | 통과 | (TESTING 실행 시 기록) |
| 4 | Dia2 한 줄 수정 재렌더 | 수정 줄만 재합성 | ✅ 25.2s → 3.3s |
| 5 | 앱 강제 종료 시 backend 정리 | python 잔존 0 | ✅ |
| 6 | 구간 json 연속성 | 빈틈 · 역전 0 | ✅ |
